.vercel

# Build artifacts — produced by `prebuild` (scripts/bundle-stories.mjs).
# Never committed; every Vercel deploy bundles fresh from
# stories-remotion/src/Root.tsx into here, then vite copies it into dist/.
public/stories-bundle/
