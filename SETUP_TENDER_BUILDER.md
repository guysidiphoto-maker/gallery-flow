# מדריך התקנה — חיפוש תמונות למכרז

## מה נבנה

טאב חדש בדשבורד הלקוח. חברת ההפקה מדביקה את תיאור המכרז שקיבלה, המערכת מוצאת תמונות רלוונטיות מהגלריות הקיימות, ומייצרת PDF עם התמונות — מוכן לשליחה ללקוח.

## דרישות מקדימות

- צריך **מפתח API של Anthropic** — נכנסים לכתובת https://console.anthropic.com/settings/keys ויוצרים מפתח חדש
- צריך **חשבון Vercel** — אותו חשבון שמחובר ל-pixflow-ai.com

---

## שלבי התקנה

### שלב 1 — התקנת כלי שורת הפקודה של Vercel

פותחים טרמינל ומריצים:

```bash
npm i -g vercel
```

### שלב 2 — התחברות לחשבון Vercel

מריצים בטרמינל:

```bash
vercel login
```

ייפתח חלון בדפדפן — מתחברים עם החשבון שמנהל את pixflow-ai.com.

### שלב 3 — קישור הפרויקט

מריצים בטרמינל:

```bash
cd /Users/guysidi/gallery-flow/gallery-web
vercel link
```

כשישאלו — בוחרים את הפרויקט הקיים (זה שמופיע ב-pixflow-ai.com).

### שלב 4 — הוספת מפתח ה-API

מריצים בטרמינל:

```bash
vercel env add ANTHROPIC_API_KEY
```

המערכת תשאל:
1. **מה הערך?** — מדביקים את מפתח ה-API שהעתקתם מ-Anthropic
2. **לאיזה סביבות?** — בוחרים את כולן (Production, Preview, Development)

### שלב 5 — הרצה מקומית

מריצים בטרמינל:

```bash
vercel dev
```

זה מפעיל את האתר מקומית. בטרמינל יופיע כתובת (בדרך כלל http://localhost:3000).

### שלב 6 — בדיקה

1. פותחים את הדשבורד של הלקוח בדפדפן (אותו URL שנכנסתם אליו קודם ב-pixflow-ai.com)
2. בתפריט העליון יופיע טאב חדש בשם **"חיפוש למכרז"** — לוחצים עליו
3. כותבים בתיבת הטקסט, לדוגמה: `כנס בינלאומי ל-5000 איש עם הרצאות ופאנלים`
4. לוחצים על **"נתח ומצא גלריות"**
5. המערכת תנתח את הטקסט, תמצא גלריות ותמונות מתאימות
6. אפשר לבחור גלריות ותמונות ספציפיות
7. לוחצים על **"הורד PDF"** כדי לייצר PDF עם התמונות שנבחרו

---

## העלאה לפרודקשן

אחרי שהכל עובד מקומית, מריצים:

```bash
vercel --prod
```

או פשוט עושים push ל-git — Vercel מעלה אוטומטית.

---

## קבצים שנוצרו

| קובץ | מה הוא עושה |
|------|-------------|
| `gallery-web/api/analyze-tender.ts` | שרת — שולח את טקסט המכרז ל-Claude AI, מחזיר סוגי אירועים וגודל |
| `gallery-web/api/generate-pitch.ts` | שרת — מייצר תיאורים בעברית לגלריות שנמצאו |
| `gallery-web/src/components/TenderBuilder.tsx` | הממשק — טופס קלט, התאמת גלריות, בחירת תמונות |
| `gallery-web/src/lib/pitchPdf.ts` | מייצר את קובץ ה-PDF מהתמונות שנבחרו |

## קבצים ששונו

| קובץ | מה השתנה |
|------|----------|
| `gallery-web/src/pages/ClientDashboard.tsx` | נוסף טאב "חיפוש למכרז" |
| `gallery-web/package.json` | נוספו חבילות `jspdf` ו-`@anthropic-ai/sdk` |
